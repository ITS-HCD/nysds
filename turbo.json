{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": [
        "src/**/*.ts",
        "src/**/*.scss",
        "src/**/*.json",
        "vite.config.js",
        "ng-packagr.json",
        "tsconfig.json",
        "tsconfig.lib.json",
        "terrazzo.config.js"
      ],
      "outputs": ["dist/**", ".turbo/**/*.xml"],
      "env": ["NODE_ENV", "BUILD_FORMAT"],
      "outputLogs": "new-only"
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
