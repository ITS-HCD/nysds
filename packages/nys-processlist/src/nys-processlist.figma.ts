import figma, { html } from "@figma/code-connect/html";

figma.connect("<FIGMA_PROCESSLIST>", {
  props: {
    // TODO: Add props here
  },
  example: () => html` <nys-processlist></nys-processlist> `,
});
