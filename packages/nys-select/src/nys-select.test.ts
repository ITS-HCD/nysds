/*** Accessibility tests ***/
/*
 * Ensure select has a label property to provide accessible text for screen readers:
 * - If description exist, it should be used readable for screen readers.
 * - If text contains prop "optional", make sure it is readable to screen readers.
 */

/*
 * Ensure Select/options is focusable and keyboard accessibility.
 */

/*
 * Test for required attribute:
 * - If the select is required, ensure that the required message is readable for screen readers.
 */

/*
 * Test for disabled attribute:
 * - If the select is disabled, ensure that the disabled state is readable for screen readers. ❌ (we currently do not)
 */
