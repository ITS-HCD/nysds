import { Meta, Story, Source, Canvas, Controls } from "@storybook/blocks";
import { html } from "lit";
import * as NysDropdownStories from "./nys-dropdown.stories";

<Meta of={NysDropdownStories} title="Components/Dropdown/Documentation" />

# Dropdown

The **`nys-dropdown`** component is a reusable web component for use in New York State digital products.

If an id is not passed, a unique id will be generated.

<p className="pill pill--stable">Stable</p>

<p className="pill pill--uikit">
	<a href="https://www.figma.com/design/0ogYpymUPQQfhELthntNbt/%F0%9F%92%A0-NYSDS-%2F-Components?node-id=730-1677&m=dev">View in Figma</a>
</p>

---

<Canvas of={NysDropdownStories.Basic} />

<Controls />

## Options

## Usage

### When to use this component

- When users need to 

### When to consider something else

<div className="nys-grid-row nys-grid-gap-lg">
	<div className="nys-mobile-lg:nys-grid-col">
		<div className="usage-card usage-card--do">
			### <nys-icon name="check_circle" color="--var(nys-color-success)" /> Do - When users need to 
		</div>
	</div>
	<div className="nys-mobile-lg:nys-grid-col">
		<div className="usage-card usage-card--dont">
			### <nys-icon name="cancel" color="--var(nys-color-error)" /> Don't - When users need to
		</div>
	</div>
</div>

## Accessibility

The `nys-dropdown` component includes the following accessibility-focused features:

- Proper ARIA roles and attributes to ensure screen readers can interpret the checkbox correctly.
- Keyboard navigation support, allowing users to use the keyboard to open and close the dropdown.
- Visual focus indicators to help users navigate the component.

## Events

The `nys-dropdown` component emits the following events:

1. `click`: Emitted when the dropdown is clicked.
2. `focus`: Emitted when the dropdown receives focus.
3. `blur`: Emitted when the dropdown loses focus.

You can listen to these events using JavaScript:

```js
// Select the dropdown component
const dropdown = document.querySelector("nys-dropdown");

// Listen for the 'click' event
dropdown.addEventListener("click", () => {
	console.log("Dropdown clicked");
});

// Listen for the 'focus' event
dropdown.addEventListener("focus", () => {
	console.log("Dropdown is focused");
});

// Listen for the 'blur' event
dropdown.addEventListener("blur", () => {
	console.log("Dropdown lost focus");
});
```

## Dependencies

This component automatically imports the following dependencies.

- `<nys-icon>`
