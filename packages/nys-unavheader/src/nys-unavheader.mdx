import { Meta, Story, Source, Canvas, Controls } from '@storybook/blocks';
import { html } from 'lit';
import * as NysUnavHeaderStories from './nys-unavheader.stories';
import "@nys-excelsior/nys-icon";

<Meta of={NysUnavHeaderStories} title="Components/UnavHeader/Documentation" />

# Universal Header

The **`nys-unavheader`** component is a reusable web component for use in New York State digital products. It helps provide users with providing users with access to essential links across all pages.

<p className="pill pill--stable">Stable</p>

<p className="pill pill--uikit"><a target="_blank" href="">View in Figma</a></p>

***
