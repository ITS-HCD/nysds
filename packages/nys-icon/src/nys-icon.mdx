import { Meta, Story, Source, Canvas } from "@storybook/blocks";
import { html } from "lit";
import * as NysIconStories from "./nys-icon.stories";

<Meta of={NysIconStories} title="Components/Icon/Documentation" />

# Icon

The **`nys-icon`** component is a reusable web component for use in New York State digital products. It provides users with the ability to choose a specific svg icon to use.

### SVG props implementations:
        * Custom size naming variants
        * Color 
        * Rotation (Degree)

### All available SVG names (Count: {26})
<div style={{ display: 'flex' }}>
  <div style={{ flex: '1 1 50%', paddingRight: '1rem' }}>
    - edit-square  
    - publish  
    - download  
    - download-done  
    - upload-file  
    - drive-folder-upload  
    - more-vert  
    - arrow-back  
    - arrow-left  
    - arrow-right  
    - arrow-down  
    - arrow-up  
    - check  
  </div>
  <div style={{ flex: '1 1 50%' }}>
    - calendar-month  
    - warning  
    - check-circle  
    - cancel  
    - close  
    - language  
    - search  
    - menu  
    - visibility  
    - visibility-off  
    - account-circle  
    - help  
    - share  
  </div>
</div>

## SVG Name Properties
### Required Property
The name property is the **ONLY** required attribute for the icon component. The
current icon name is "publish" but you can change it to any of the listed icons like "edit-square".

Note: Leaving the 'label' blank will set aria-hidden for screen readers.

<Canvas of={NysIconStories.NameProp} />

### Labels
Here are some of the features that come with the icon component. Keep scrolling down for further details.
<Canvas of={NysIconStories.Labels} />


## Icon Property Examples

### Size Inheritance
The icon size will inherit the relative font size from its nearest parent. 

<Canvas of={NysIconStories.InheritSize} />

### Size Naming Variant
To scale an icon relative to the inherited font size, pass a size variant like `size=[variant name]` as a prop:
 - `xs (extra small)`
 - `sm (small)`
 - `md (medium)`
 - `lg (large)`
 - `xl (extra large)`

Each size variant (xs, sm, md, lg, xl) scales the icon’s height relative to the parent font size, from smallest (xs) to largest (xl).
<Canvas of={NysIconStories.CustomSize} />

### Color Inheritance
An icon will inherit the current text color from its closest parent element.
<Canvas of={NysIconStories.ColorInheritance} />

### Color Change Using CSS Custom Variable
You can override any parents inheritance by setting a color prop.
<Canvas of={NysIconStories.ColorChange} />

### Rotate
Set the icon's rotation by passing the angle as a number, like rotate="20" (without "deg").
<Canvas of={NysIconStories.Rotate} />


## Accessibility

The `nys-icon` component includes the following accessibility-focused features:

- **ARIA Hidden by Default:** If no label is provided, the icon is hidden from screen readers by setting `aria-hidden="true"`.
- **Customizable ARIA Label:** If a label is provided, the component automatically adds an `aria-label` attribute, making the icon accessible to screen readers.