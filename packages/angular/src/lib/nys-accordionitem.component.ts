/* AUTO-GENERATED by src/scripts/cem-angular-plugin.mjs — do not edit.
 * Regenerate with `npm run cem`. */

import {
  ChangeDetectionStrategy,
  Component,
  ElementRef,
  NgZone,
} from "@angular/core";
import type { Observable } from "rxjs";
import { ProxyCmp, proxyOutputs } from "./utils";
import type { NysAccordionItem as NysAccordionItemElement } from "@nysds/nys-accordion";
import "@nysds/nys-accordion";

@ProxyCmp({ inputs: ["bordered", "expanded", "heading", "headingLevel", "id"] })
@Component({
  selector: "nys-accordionitem",
  template: "<ng-content></ng-content>",
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  inputs: ["bordered", "expanded", "heading", "headingLevel", "id"],
  outputs: ["nys-accordionitem-toggle"],
})
export class NysAccordionItemComponent {
  protected readonly el: NysAccordionItemElement;

  constructor(
    elementRef: ElementRef,
    protected readonly z: NgZone,
  ) {
    this.el = elementRef.nativeElement;
    proxyOutputs(this, this.el, ["nys-accordionitem-toggle"]);
  }
}

export declare interface NysAccordionItemComponent extends Pick<
  NysAccordionItemElement,
  "bordered" | "expanded" | "heading" | "headingLevel" | "id"
> {
  "nys-accordionitem-toggle": Observable<CustomEvent>;
}
