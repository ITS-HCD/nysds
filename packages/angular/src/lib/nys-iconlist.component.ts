/* AUTO-GENERATED by src/scripts/cem-angular-plugin.mjs — do not edit.
 * Regenerate with `npm run cem`. */

import {
  ChangeDetectionStrategy,
  ChangeDetectorRef,
  Component,
  ElementRef,
  NgZone,
} from "@angular/core";
import { ProxyCmp } from "./utils";
import type { NysIconlist as NysIconlistElement } from "@nysds/nys-iconlist";
import "@nysds/nys-iconlist";

@ProxyCmp({ inputs: ["divider", "id"] })
@Component({
  selector: "nys-iconlist",
  template: "<ng-content></ng-content>",
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  inputs: ["divider", "id"],
})
export class NysIconlistComponent {
  protected readonly el: NysIconlistElement;

  constructor(
    changeDetector: ChangeDetectorRef,
    elementRef: ElementRef,
    protected readonly z: NgZone,
  ) {
    // The wrapper renders nothing of its own (ng-content only); the custom
    // element manages its own rendering, so Angular CD is detached entirely.
    changeDetector.detach();
    this.el = elementRef.nativeElement;
  }
}

export declare interface NysIconlistComponent extends Pick<
  NysIconlistElement,
  "divider" | "id"
> {}
