/* AUTO-GENERATED by src/scripts/cem-angular-plugin.mjs — do not edit.
 * Regenerate with `npm run cem`. */

import {
  ChangeDetectionStrategy,
  ChangeDetectorRef,
  Component,
  ElementRef,
  NgZone,
} from "@angular/core";
import { ProxyCmp } from "./utils";
import type { NysGlobalHeader as NysGlobalHeaderElement } from "@nysds/nys-globalheader";
import "@nysds/nys-globalheader";

@ProxyCmp({
  inputs: [
    "agencyName",
    "appName",
    "homepageLink",
    "id",
    "landmarkLabel",
    "nysLogo",
  ],
})
@Component({
  selector: "nys-globalheader",
  template: "<ng-content></ng-content>",
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  inputs: [
    "agencyName",
    "appName",
    "homepageLink",
    "id",
    "landmarkLabel",
    "nysLogo",
  ],
})
export class NysGlobalHeaderComponent {
  protected readonly el: NysGlobalHeaderElement;

  constructor(
    changeDetector: ChangeDetectorRef,
    elementRef: ElementRef,
    protected readonly z: NgZone,
  ) {
    // The wrapper renders nothing of its own (ng-content only); the custom
    // element manages its own rendering, so Angular CD is detached entirely.
    changeDetector.detach();
    this.el = elementRef.nativeElement;
  }
}

export declare interface NysGlobalHeaderComponent extends Pick<
  NysGlobalHeaderElement,
  "agencyName" | "appName" | "homepageLink" | "id" | "landmarkLabel" | "nysLogo"
> {}
