/* AUTO-GENERATED by src/scripts/cem-angular-plugin.mjs — do not edit.
 * Regenerate with `npm run cem`. */

import {
  ChangeDetectionStrategy,
  ChangeDetectorRef,
  Component,
  ElementRef,
  HostListener,
  NgZone,
  forwardRef,
} from "@angular/core";
import { NG_VALUE_ACCESSOR } from "@angular/forms";
import type { ControlValueAccessor } from "@angular/forms";
import type { Observable } from "rxjs";
import { ProxyCmp, proxyOutputs } from "./utils";
import type { NysTextinput as NysTextinputElement } from "@nysds/nys-textinput";
import "@nysds/nys-textinput";

@ProxyCmp({
  inputs: [
    "ariaLabel",
    "description",
    "disabled",
    "errorMessage",
    "form",
    "id",
    "inverted",
    "label",
    "max",
    "maxlength",
    "min",
    "name",
    "optional",
    "pattern",
    "placeholder",
    "readonly",
    "required",
    "showError",
    "step",
    "tooltip",
    "type",
    "value",
    "width",
  ],
})
@Component({
  selector: "nys-textinput",
  template: "<ng-content></ng-content>",
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  inputs: [
    "ariaLabel",
    "description",
    "disabled",
    "errorMessage",
    "form",
    "id",
    "inverted",
    "label",
    "max",
    "maxlength",
    "min",
    "name",
    "optional",
    "pattern",
    "placeholder",
    "readonly",
    "required",
    "showError",
    "step",
    "tooltip",
    "type",
    "value",
    "width",
  ],
  outputs: ["nys-blur", "nys-focus", "nys-input"],
  providers: [
    {
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => NysTextinputComponent),
      multi: true,
    },
  ],
})
export class NysTextinputComponent implements ControlValueAccessor {
  protected readonly el: NysTextinputElement;

  constructor(
    changeDetector: ChangeDetectorRef,
    elementRef: ElementRef,
    protected readonly z: NgZone,
  ) {
    // The wrapper renders nothing of its own (ng-content only); the custom
    // element manages its own rendering, so Angular CD is detached entirely.
    changeDetector.detach();
    this.el = elementRef.nativeElement;
    proxyOutputs(this, this.el, ["nys-blur", "nys-focus", "nys-input"]);
  }

  // ControlValueAccessor — bridges Angular forms (ngModel / formControlName)
  // to the element's `value` property and its `nys-input` event.
  private onChange: (value: any) => void = () => {};
  private onTouched: () => void = () => {};

  writeValue(value: any): void {
    this.el.value = value ?? "";
  }

  registerOnChange(fn: (value: any) => void): void {
    this.onChange = fn;
  }

  registerOnTouched(fn: () => void): void {
    this.onTouched = fn;
  }

  setDisabledState(isDisabled: boolean): void {
    this.el.disabled = isDisabled;
  }

  @HostListener("nys-input")
  protected handleChangeEvent(): void {
    this.onChange(this.el.value);
  }

  @HostListener("nys-blur")
  protected handleBlurEvent(): void {
    this.onTouched();
  }
}

export declare interface NysTextinputComponent extends Pick<
  NysTextinputElement,
  | "ariaLabel"
  | "description"
  | "disabled"
  | "errorMessage"
  | "form"
  | "id"
  | "inverted"
  | "label"
  | "max"
  | "maxlength"
  | "min"
  | "name"
  | "optional"
  | "pattern"
  | "placeholder"
  | "readonly"
  | "required"
  | "showError"
  | "step"
  | "tooltip"
  | "type"
  | "value"
  | "width"
> {
  "nys-blur": Observable<CustomEvent>;
  "nys-focus": Observable<CustomEvent>;
  "nys-input": Observable<CustomEvent>;
}
