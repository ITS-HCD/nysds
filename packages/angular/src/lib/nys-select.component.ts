/* AUTO-GENERATED by src/scripts/cem-angular-plugin.mjs — do not edit.
 * Regenerate with `npm run cem`. */

import {
  ChangeDetectionStrategy,
  Component,
  ElementRef,
  HostListener,
  NgZone,
  forwardRef,
} from "@angular/core";
import { NG_VALUE_ACCESSOR } from "@angular/forms";
import type { ControlValueAccessor } from "@angular/forms";
import type { Observable } from "rxjs";
import { ProxyCmp, proxyOutputs } from "./utils";
import type { NysSelect as NysSelectElement } from "@nysds/nys-select";
import "@nysds/nys-select";

@ProxyCmp({
  inputs: [
    "ariaLabel",
    "description",
    "disabled",
    "errorMessage",
    "form",
    "id",
    "inverted",
    "label",
    "name",
    "optional",
    "required",
    "showError",
    "tooltip",
    "value",
    "width",
  ],
})
@Component({
  selector: "nys-select",
  template: "<ng-content></ng-content>",
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  inputs: [
    "ariaLabel",
    "description",
    "disabled",
    "errorMessage",
    "form",
    "id",
    "inverted",
    "label",
    "name",
    "optional",
    "required",
    "showError",
    "tooltip",
    "value",
    "width",
  ],
  outputs: ["nys-blur", "nys-change", "nys-focus"],
  providers: [
    {
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => NysSelectComponent),
      multi: true,
    },
  ],
})
export class NysSelectComponent implements ControlValueAccessor {
  protected readonly el: NysSelectElement;

  constructor(
    elementRef: ElementRef,
    protected readonly z: NgZone,
  ) {
    this.el = elementRef.nativeElement;
    proxyOutputs(this, this.el, ["nys-blur", "nys-change", "nys-focus"]);
  }

  // ControlValueAccessor — bridges Angular forms (ngModel / formControlName)
  // to the element's `value` property and its `nys-change` event.
  private onChange: (value: any) => void = () => {};
  private onTouched: () => void = () => {};

  writeValue(value: any): void {
    this.el.value = value ?? "";
  }

  registerOnChange(fn: (value: any) => void): void {
    this.onChange = fn;
  }

  registerOnTouched(fn: () => void): void {
    this.onTouched = fn;
  }

  setDisabledState(isDisabled: boolean): void {
    this.el.disabled = isDisabled;
  }

  @HostListener("nys-change")
  protected handleChangeEvent(): void {
    this.onChange(this.el.value);
  }

  @HostListener("nys-blur")
  protected handleBlurEvent(): void {
    this.onTouched();
  }
}

export declare interface NysSelectComponent extends Pick<
  NysSelectElement,
  | "ariaLabel"
  | "description"
  | "disabled"
  | "errorMessage"
  | "form"
  | "id"
  | "inverted"
  | "label"
  | "name"
  | "optional"
  | "required"
  | "showError"
  | "tooltip"
  | "value"
  | "width"
> {
  "nys-blur": Observable<CustomEvent>;
  "nys-change": Observable<CustomEvent>;
  "nys-focus": Observable<CustomEvent>;
}
