import { Directive } from '@angular/core';

// TODO(task-7): registered alongside `nys-dropdownmenu`.
// TODO(task-8): generator populates typed @Input()s/@Output()s from the CEM.

/** Wrapper directive for `<nys-dropdownmenuitem>`. */
@Directive({
  selector: 'nys-dropdownmenuitem',
  standalone: true,
})
export class NysDropdownmenuitemDirective {}
