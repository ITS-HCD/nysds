import { Directive } from '@angular/core';

// TODO(task-7): add `import '@nysds/nys-backtotop';` once component dist exists.
// TODO(task-8): generator populates typed @Input()s/@Output()s from the CEM.

/** Wrapper directive for `<nys-backtotop>`. */
@Directive({
  selector: 'nys-backtotop',
  standalone: true,
})
export class NysBacktotopDirective {}
