/* AUTO-GENERATED by src/scripts/cem-angular-plugin.mjs — do not edit.
 * Regenerate with `npm run cem`. */

import {
  ChangeDetectionStrategy,
  ChangeDetectorRef,
  Component,
  ElementRef,
  NgZone,
} from "@angular/core";
import type { Observable } from "rxjs";
import { ProxyCmp, proxyOutputs } from "./utils";
import type { NysPagination as NysPaginationElement } from "@nysds/nys-pagination";
import "@nysds/nys-pagination";

@ProxyCmp({ inputs: ["currentPage", "id", "name", "totalPages"] })
@Component({
  selector: "nys-pagination",
  template: "<ng-content></ng-content>",
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  inputs: ["currentPage", "id", "name", "totalPages"],
  outputs: ["nys-change"],
})
export class NysPaginationComponent {
  protected readonly el: NysPaginationElement;

  constructor(
    changeDetector: ChangeDetectorRef,
    elementRef: ElementRef,
    protected readonly z: NgZone,
  ) {
    // The wrapper renders nothing of its own (ng-content only); the custom
    // element manages its own rendering, so Angular CD is detached entirely.
    changeDetector.detach();
    this.el = elementRef.nativeElement;
    proxyOutputs(this, this.el, ["nys-change"]);
  }
}

export declare interface NysPaginationComponent extends Pick<
  NysPaginationElement,
  "currentPage" | "id" | "name" | "totalPages"
> {
  "nys-change": Observable<CustomEvent>;
}
